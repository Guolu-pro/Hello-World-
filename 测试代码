#include "pch.h"
#include "CppUnitTest.h"
#include "F:\桌面\vs2019\算数题\test.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace UnitTest1
{
	TEST_CLASS(UnitTest1)
	{
	public:
		TEST_METHOD(TestMethod1)
		{
			int demical = 0;
			int Max = 10;
			Assert::AreEqual(0, randomNumber(demical, Max));
		}
		TEST_METHOD(TestMethod2)
		{
			int demical = 1;
			int Max = 10;
			Assert::AreEqual(0, randomNumber(demical, Max));
		}TEST_METHOD(TestMethod3)
		{
			int brackets = 0;
			Assert::AreEqual(0, randomBrackets(brackets));
		}
		
	};
}
